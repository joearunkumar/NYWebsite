export declare class Credentials {
    private _config;
    private _credentials;
    private _credentials_source;
    private _gettingCredPromise;
    private _authClass;
    private _cacheClass;
    private _refreshHandlers;
    constructor(config: any);
    setAuthClass(auth: any): void;
    setCacheClass(cache: any): void;
    getCredSource(): any;
    configure(config: any): any;
    get(): any;
    private _pickupCredentials();
    private _keepAlive();
    refreshFederatedToken(federatedInfo: any): any;
    private _isExpired(credentials);
    private _setCredentialsForGuest();
    private _setCredentialsFromAWS();
    private _setCredentialsFromFederation(params);
    private _setCredentialsFromSession(session);
    private _loadCredentials(credentials, source, authenticated, info);
    set(params: any, source: any): Promise<{}>;
    clear(): Promise<void>;
}
declare const instance: Credentials;
export default instance;
